# use alphabets(a-z),underscore(_) in biggning
a=3
_name='idha'
print(a,_name)

# don't start from a number  (6tables can not be assigned)
#9tables=1                 #error


# don't assign a name that is already defined in the library
#for=2                     #error
#print(for)

# use underscore to separate words (per_table_value)
per_table_value=12
print(per_table_value)